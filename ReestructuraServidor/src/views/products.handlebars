<script src="/js/index.js"></script>

<header>
    <nav class="navbar navbar-expand-xl">
        <div class="container">
            <a class="navbar-brand brand header-adj" href="http://localhost:8080/">
                Tienda genérica.
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse text-center justify-content-end" id="navbarNav">
                <!-- paginas -->
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link blanco" aria-current="page" href="http://localhost:8080/products">Productos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link blanco" aria-current="page" href="http://localhost:8080/profile">Perfil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link blanco" href="http://localhost:8080/cart">🛒</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<h1 class="title">Bienvenido {{user}} - Rol: {{role}}</h1>
{{!-- <form action="/logout" method="POST">
    <button type="submit" class="btn btn-outline-secondary">Cerrar Sesión</button>
</form> --}}
<div class="items">
    <article class="itemList container">
        <div class="row">
            <h2> Productos </h2>
        </div>
        <div class="row row-gap-2">
            {{#each result.docs}}
            <div class="col-xl-4 col-md-6 col-xs-12">
                <div class="itemCard">
                    <div class="item"></div>
                    <h3> {{this.title}}</h3>
                    <h4> ${{this.price}} CLP</h4>
                    <button type="button" class="btn btn-outline-secondary btn-lg" onclick="addproduct({{this.id}},{{../cartId}})">🛒</button>
                </div>
            </div>
            {{/each}}
        </div>
    </article>
</div>
<div class="botones-pag align-items-center justify-content-evenly d-flex flex-row">
    <a href={{result.prevLink}}>
        <button class="btn btn-outline-secondary btn-page"
        {{#unless result.hasPrevPage}}
        disabled
        {{/unless}}
        >←</button>
    </a>
    <p>Página: {{result.page}}</p>
    <a href={{result.nextLink}}>
        <button class="btn btn-outline-secondary btn-page"
        {{#unless result.hasNextPage}}
        disabled
        {{/unless}}
        >→
        </button>
    </a>
</div>