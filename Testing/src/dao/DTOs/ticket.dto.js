
export default class ticketDTO {
    constructor(code, time, amount, purchaser){
        this.code = code
        this.purchaseTime = time
        this.amount = amount
        this.purchaser = purchaser
    }
}